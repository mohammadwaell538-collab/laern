# Example environment variables for local development and deployment
# Copy this file to .env and fill values as appropriate

# Gemini / Google GenAI API Key (optional if you rely on local fallback)
GOOGLE_API_KEY=
GENAI_API_KEY=

# Optional Tesseract path (Windows); if installed and not in PATH, set the full path
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# Optional: path to ffmpeg binary if not in PATH
# FFMPEG_PATH=/usr/bin/ffmpeg

# Runtime config
PORT=8080

# Note: This server supports content analysis in both Arabic and English.
# Ensure your environment includes tesseract and ffmpeg if you need OCR/ASR features.