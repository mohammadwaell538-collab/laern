# 🚀 دليل سريع — تشغيل Laern

## الخطوة 1️⃣: إعداد الخادم (Python)

```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تعيين API Key (اختياري)
# Windows PowerShell:
$env:GOOGLE_API_KEY="sk-..."

# Linux/Mac:
export GOOGLE_API_KEY="sk-..."

# تشغيل الخادم على المنفذ 8080
python proxy_server.py
```

**النتيجة المتوقعة:**
```
🚀 Starting Laern Analysis Server on port 8080...
📝 Gemini API configured: ✓
INFO:     Uvicorn running on http://0.0.0.0:8080
```

## الخطوة 2️⃣: تشغيل التطبيق (Flutter)

```bash
# تشغيل على المتصفح
flutter run -d chrome

# أو على ويب كامل
flutter run -d web-server
```

## الخطوة 3️⃣: الاستخدام

1. **في HomePage**:
   - أدخل نص تعليمي
   - أضف رابط YouTube (اختياري)
   - اختر نوع التحليل:
     - ✨ **استخراج المفاهيم** (concepts)
     - ❓ **توليد أسئلة** (questions)
     - 🗺️ **خريطة ذهنية** (mindmap)
     - 📖 **ملخص شامل** (summary)
   - حمّل ملف PDF (اختياري)
   - اضغط **ابدأ تحليل الذكاء الاصطناعي**

2. **في DashboardPage**:
   - اضغط أيقونة لوحة القيادة في الزاوية العلوية اليسرى
   - شاهد:
     - عدد المواضيع الرئيسية
     - عدد النقاط الأساسية
     - حالة التحليل
     - النص الكامل للتحليل

## 🔍 اختبار سريع للخادم

```bash
# من Terminal جديد (مع تشغيل الخادم)
curl -X POST http://localhost:8080/analyze \
  -F "text=الذكاء الاصطناعي هو مجال يدرس آليات الذكاء" \
  -F "youtube=" \
  -F "analysis_type=summary"
```

**الاستجابة المتوقعة:**
```json
{
  "analysis": "نص التحليل من Gemini...",
  "main_topics": 5,
  "key_points": 12
}
```

## ⚙️ معلومات المشروع

| المكون | التفاصيل |
|-------|---------|
| **الخادم** | FastAPI + Uvicorn على `0.0.0.0:8080` |
| **API** | Google Gemini 2.0 Flash |
| **التطبيق** | Flutter Web (Material 3) |
| **اللغة** | عربي (RTL) + إنجليزي |
| **المكتبات** | google-genai, file_picker, http, universal_html |

## 🆘 استكشاف الأخطاء

### الخادم لا يبدأ
```
❌ "Address already in use"
→ تغيير المنفذ: uvicorn.run(..., port=8081)
```

### التطبيق لا يتصل بالخادم
```
❌ "Failed to connect"
→ تأكد: http://localhost:8080 متاح
→ تحقق: firewall/antivirus لا يحجب المنفذ
```

### لا توجد استجابة من Gemini
```
❌ "No GOOGLE_API_KEY"
→ تعيين: export GOOGLE_API_KEY="your-key"
→ أو: run النموذج بدون API (استجابة افتراضية)
```

### Flutter لا يجد الخادم على iOS/Android
```
❌ "localhost:8080" لا يعمل على الجهاز الفعلي
→ استخدم: عنوان IP الحقيقي للجهاز بدلاً من localhost
→ مثال: http://192.168.1.100:8080/analyze
```

## ✅ قائمة التحقق قبل الإطلاق

- [ ] تم تثبيت المكتبات: `pip install -r requirements.txt`
- [ ] الخادم يعمل على المنفذ 8080
- [ ] Flutter يحتوي على web support: `flutter create --platforms web`
- [ ] تم تحديث main.dart بـ URL جديد
- [ ] Google API Key معيّن (اختياري للنموذج)
- [ ] Chrome متوفر للاختبار

## 📱 الاختبار على أجهزة حقيقية

```bash
# تعديل URL الخادم:
# Android/iOS: استخدم الـ IP الحقيقية بدلاً من localhost
http://192.168.1.100:8080/analyze
```

## 🚀 النشر على Render

1. **إنشاء خدمة ويب جديدة**
   - انتقل إلى [لوحة تحكم Render](https://dashboard.render.com/).
   - انقر على "جديد" → "خدمة ويب".
   - قم بتوصيل مستودع GitHub الخاص بك.

2. **متغيرات البيئة**
   - أضف متغيرات البيئة التالية في لوحة تحكم Render:
     - `GOOGLE_API_KEY`: مفتاح API الخاص بك لـ Gemini (إذا كان متاحًا).
     - `GENAI_API_KEY`: مفتاح بديل لـ Gemini (اختياري).

3. **أمر البناء**
   ```bash
   pip install -r requirements.txt
   ```

4. **أمر البدء**
   ```bash
   python -m uvicorn proxy_server:app --host 0.0.0.0 --port 8080
   ```

5. **المنافذ**
   - تأكد من أن الخدمة تستمع على المنفذ `8080`.

6. **اعتماديات اختيارية**
   - تثبيت `Tesseract` لدعم OCR:
     - أوبونتو: `sudo apt-get install tesseract-ocr`
     - ويندوز: [تحميل المثبت](https://github.com/UB-Mannheim/tesseract/wiki)
   - تثبيت `ffmpeg` لمعالجة الصوت:
     - أوبونتو: `sudo apt-get install ffmpeg`
     - ويندوز: `choco install ffmpeg`

## GitHub Actions للنشر المستمر / التكامل المستمر

1. **إعداد سير العمل**
   - أنشئ ملف `.github/workflows/deploy.yml`:
     ```yaml
     name: Deploy to Render

     on:
       push:
         branches:
           - main

     jobs:
       deploy:
         runs-on: ubuntu-latest

         steps:
         - name: Checkout Code
           uses: actions/checkout@v3

         - name: Install Dependencies
           run: pip install -r requirements.txt

         - name: Run Tests
           run: python scripts/test_mock_uploads.py

         - name: Deploy to Render
           env:
             RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
           run: |
             curl -X POST \ 
             -H "Authorization: Bearer $RENDER_API_KEY" \ 
             -d '{"serviceId": "<YOUR_SERVICE_ID>", "clearCache": true}' \ 
             https://api.render.com/v1/services/<YOUR_SERVICE_ID>/deploys
     ```

2. **الأسرار**
   - أضف `RENDER_API_KEY` إلى أسرار مستودع GitHub الخاص بك.

3. **اختبار محلي**
   - قم بتشغيل `python scripts/test_mock_uploads.py` للتأكد من نجاح جميع الاختبارات قبل دفع التغييرات.

---

**🎉 مستعد للإطلاق!**

هذا الخادم يدعم كل من **التحليل العربي** و **الإنجليزي**. تأكد من أن بيئة النشر لديك تحتوي على المكتبات اللازمة لضمان الوظائف الكاملة.
